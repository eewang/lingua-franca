import React from 'react';

export const Foo = React.createClass({
  render() {
    return (
      <div>Foobar</div>
    );
  }
});

// Give me [a] nouns, [b] verbs, [c] adjectives, [d] concept, and [e] story prompt
